# Rotina para criação de variável Y
X <- 2.5 * 4    # Cria um objeto (variável)
X               # Use Ctrl+Espaço p/ auto complete (operações e documentação)
Y <- X ^ 2      # (ver aba History de comandos e formas de uso - Explore)

# Exemplo 2 - Primeiras funções matemáticas
log(1)          # logaritmo
raiz.quad <- sqrt(Y) # Y deve estar armazenado
round(5.34999,2) # arredondamento valor c/ 02 casas decimais
round(digits=2,x=5.34999) # alternativa p/ passar parâmetros
?round # verificar os parâmetros de uma função
??inner_join #procurar ajuda de funções que não estão "instaladas" ainda